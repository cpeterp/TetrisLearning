name: ./.conda
channels:
  - defaults
dependencies:
  - pip
  - python=3.8
  - pip:
      - pyboy==1.6.14
      - stable-baselines3==2.2.1
      - torch==2.2.0
      - torchaudio==2.2.0
      - torchvision==0.17.0
prefix: ./.conda
